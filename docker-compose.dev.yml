version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: user-management-dev

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev -- --host